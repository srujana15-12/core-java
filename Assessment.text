1) Explain the Constructor chaining using this() with example.
 calling one constructor from another constructor in same class using class using this keyword
 
class Terminal 
{
    int terminalId;
    String terminalName;

    Terminal(int id, String name) 
	{
        terminalId = id;
        terminalName = name;
    }
}

class Airport
 {
    int airportId;
    String airportName;
    Terminal terminal;

   
    Airport(int id, String name, int tid, String tname) 
	{
        airportId = id;
        airportName = name;
        terminal = new Terminal(tid, tname);
    }

    void display() 
	{
        System.out.println(airportId + " " + airportName);
        System.out.println(terminal.terminalId + " " + terminal.terminalName);
    }
}

class AirportExecutor
{
    public static void main(String[] args) 
	{
        Airport a = new Airport(1, "Kempegowda", 101, "T1");
        a.display();
    }
}

2} Explain default values assigned to instance variable in java with example.
 
Answer:
 when we don't initialize any values, the java compiler automatically assigns a default values to it.
some of the default values are:
int-0;
String-Null;
Boolean-false etc..

class Airport 
{
    int airportId;
    String airportName;

    void display() 
	{
        System.out.println(airportId);   
        System.out.println(airportName); 
    }
}

class AirportExecutor 
{
    public static void main(String[] args) 
	{
        Airport a = new Airport();
        a.display();
    }
}


3. Write a java program to perform CRUD operations on an array:
	Create an array
	Read/display elements
	Update an element
	delete an element

Answer:
class Country
{
	String states[]=new String[29];
	int index;
	
	public boolean addStateNames(String state)
	{
		boolean isStatesAdded=false;
		if(state!=null && !state.isEmpty())
		{
			states[index++]=state;
			isStatesAdded=true;
		}
		else
			System.out.println(state+"state not found");
			
		return isStatesAdded;
	}
	
	public void getStateDetails()
	{
		System.out.println("The list of States are:");
		for(String country:states)
			System.out.println(country);
	}
	public String getStateByName(String stateName)
	{
		String name=null;
		for(String state:states)
		{
			if(state==stateName)
			{
				name=state;
				break;
			}
		}
		if(name==null)
			System.out.println(stateName+" not found");
		
		return name;
	}
	
	
	public boolean updateState(String existingState,String updatedState)
	{
		boolean isStateUpdated=false;
		
		for(int index=0;index<states.length;index++)
		{
			if(states[index]==existingState)
			{
				states[index]=updatedState;
				isStateUpdated=true;
			}
		}
		
		if(isStateUpdated==false)
			System.out.println(existingState+" is not found");
		
		return isStateUpdated;
	}
	
	
	public boolean deleteState(String deleteState)
	{
		boolean isStateDeleted=false;
		
		for(int index=0;index<states.length;index++)
		{
			if(states[index]==deleteState)
			{
				states[index]=null;
				isStateDeleted=true;
			}
		}
		
		if(isStateDeleted==false)
			System.out.println(deleteState+" state not found");
		
		return isStateDeleted;
	}
}


4. Write a program to demonstrate a default constructor.
Answer:
class Politician 
{
    int politicianId;
    Voter voter;

    public void getPoliticianDetails() 
	{
        System.out.println("the politician id is: " + politicianId);
    }

    Politician(int politicianId, String name, Voter voter) 
	{
        this.politicianId=politicianId;
    }

    Politician() 
	{

    }

    public boolean createVoter(Voter voter) 
	{
        this.voter=voter;
        return true;
    }
}
class Voter 
{
    int voterId;

    public void getVoterDetails() 
	{
        System.out.println("the voter id is: " + voterId);
    }
}

class PoliticianOneExecutor 
{
    public static void main(String[] args) 
	{
        Politician politician=new Politician();
        politician.politicianId=1;

        Voter voter=new Voter();
        voter.voterId=501;

        politician.voter=voter;

        politician.getPoliticianDetails();
    }
}


5.Write a program using a parameterized constructor 
Answer:
Class Country
{
	Country country[]= new Country[];
	String name;
	
	public boolean getStateDetails(Country country, String name)
	{
		this.country=country;
		this.name="Ind";
	}
}

class CountryExecutor
{
	public static void main(String[] args)
	{
		Country count=new Country();
		
		count.getStateDetails();
	}
}



6. What is a Constructor in java? Who provides the default Constructor?
 Constructor is a special type of method where constructor is having same name as class name.
 Java compiler provides the default constructor. 
 
 
7. Can constructor have a return type? Why?
	No, constructor don't have return type because it's a function not a method.
	
	
8. Give a real world example of has-a-relationship
Answer:


class Airport
{
	int airportId;
	String airportName;
	String airportAddress;
	Terminal terminal;
	

	Airport(int airportId, String airportName, String airportAddress)
	{
		this.airportId=airportId;
		this.airportName=airportName;
		this.airportAddress=airportAddress;
	}
	public void getAirportDetails()
	{	
		System.out.println("The Airport Id is: "+airportId);
		System.out.println("The Airport Name is: "+airportName);
		
		terminal.getTerminalDetails();
		
	}
	
	public boolean createTerminal(Terminal terminal)
	{
			this.terminal=terminal;
			return true;
	}
}
class Terminal
{
	int terminalId;
	String terminalName;

	Terminal(int terminalId, String terminalName)
	{
		this.terminalId=terminalId;
		this.terminalName=terminalName;
	}
	
	public void getTerminalDetails()
	{
		System.out.println("The terminal id is: "+terminalId);
		System.out.println("The terminal name is: "+terminalName);
	}
}
class AirportExecutor
{
	public static void main(String[] args)
	{
		
		Airport airport=new Airport();
		airport.airportId=1;
		airport.airportName="Kempegowda";
		
		Terminal ter=new Terminal();
		ter.terminalId=11;
		ter.terminalName="Terminal 1";
		
		airport.terminal=ter;
		
		airport.getAirportDetails();
	}
}

